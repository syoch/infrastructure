"""Portal control-plane device agent (standalone distribution).

This package intentionally depends only on ``websockets`` and ``jsonschema`` so
it can be deployed to devices without the portal backend dependencies.
"""
